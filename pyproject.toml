[project]
name = "Siphon"
version = "1.5.3"
description = "Siphon is a TUI audio/video downloader based on yt-dlp"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "yt-dlp",
    "textual",
    "mutagen"
]
